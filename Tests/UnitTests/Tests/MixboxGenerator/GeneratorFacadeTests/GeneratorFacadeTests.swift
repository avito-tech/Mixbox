import XCTest
import MixboxGenerators
import MixboxStubbing

// Notes:
//
// - A useful one-liner to generate random ints  (in `swift` cli app):
//   `Int64.random(in: Int64.min...Int64.max)`
// - For strings (in unix shell):
//   `uuidgen`
//
class BaseGeneratorFacadeTests: BaseGeneratorTestCase {
}
